{
    "buildCommand": "prisma generate && next build",
    "version": 2,
    "routes": [
      {
        "src": "/(.*)",
        "headers": {
          "cache-control": "public, max-age=0, must-revalidate"
        }
      }
    ]
  }